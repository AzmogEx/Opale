// SPA pure : pas de SSR (l'app parle à l'API du homelab avec un jeton
// stocké localement), pas de prérendu (tout est derrière l'authentification).
export const ssr = false;
export const prerender = false;
